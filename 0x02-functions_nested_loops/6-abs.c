#include "main.h"
/**
 * main - Point
 *
 * Description: A C program that prints with puts function
 *
 * Return: Always 0 (success)
*/
int _abs(int);
{
int num;
if (num < 0)
{
return (-num);
}
else
{
return (num);
}
}

