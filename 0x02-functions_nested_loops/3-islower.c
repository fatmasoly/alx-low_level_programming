#include "main.h"
/**
 * main - Point
 *
 * Description: A C program that prints with puts function
 *
 * Return: Always 0 (success)
*/
int _islower(int c);
{
if (c >= 97 && c <= 122)
return (1);
else
return (0);
}

